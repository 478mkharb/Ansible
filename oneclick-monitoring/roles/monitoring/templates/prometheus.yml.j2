global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "node_exporter"
    static_configs:
{% for host in groups['all'] %}
      - targets: ["{{ hostvars[host].inventory_hostname }}:9100"]
{% endfor %}
