global:
  scrape_interval: 15s

scrape_configs:
  - job_name: kubernetes-nodes
    static_configs:
      {% for host in groups['kubernetes'] %}
      - targets: ['{{ hostvars[host].ansible_host }}:9100']
      {% endfor %}
