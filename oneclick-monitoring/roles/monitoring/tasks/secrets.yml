- name: Fetch Slack webhook from AWS Secrets Manager
  set_fact:
    slack_webhook: "{{ lookup('amazon.aws.aws_secret', 'oneclick-monitoring/slack-webhook', region='ap-south-1', errors='ignore') | default('', true) }}"
