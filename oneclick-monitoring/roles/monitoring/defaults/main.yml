# Default variables for monitoring role

monitoring_dir: /opt/monitoring
prometheus_version: 2.50.0
alertmanager_version: 0.25.0
grafana_version: latest

# Slack webhook (optional, avoid template failures)
slack_webhook: ""

# Prometheus scrape targets for Kubernetes nodes
k8s_group_name: "kubernetes"
